{
  "version": 2,
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist/public",
  "installCommand": "npm ci || npm install",
  "routes": [
    { "src": "/(.*)", "dest": "/$1" }
  ]
}


